services:
  radio-spectrum:
    image: nginx:alpine
    container_name: radio-spectrum
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
